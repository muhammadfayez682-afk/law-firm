@AGENTS.md

# ميزان (Meezan) — نظام إدارة مكتب محاماة

## 1. وصف المشروع وهدفه

"ميزان" نظام إدارة داخلي لشركة **قدوم الحقائق للمحاماة والاستشارات القانونية**، مبني ليكون النظام المرجعي الوحيد لعمل المكتب اليومي: إدارة القضايا من الاستقبال حتى الأرشفة، متابعة العملاء والوكالات، جدولة الجلسات والتذكير بها، تتبّع مسارات التسوية الودية الإلزامية والاختيارية، توليد المستندات والنماذج القانونية بصيغة PDF بالهوية العربية الصحيحة، وعرض تقارير أداء للإدارة. الواجهة بالكامل عربية باتجاه RTL، والنظام داخلي (ليس منتجًا يُباع لعملاء خارجيين).

## 2. الستاك التقني

- **Next.js 16.2.10** (App Router + Turbopack) — ملاحظة: نسخة تجريبية بها تغييرات جذرية عن المعتاد (`params`/`searchParams`/`cookies()` كلها `Promise`)، راجع `AGENTS.md` قبل أي تعديل غير مألوف.
- **React 19.2.4** + **TypeScript 5** (strict)
- **Prisma 7.8.0** مع `@prisma/adapter-pg` (Driver Adapters — لا يوجد `url` داخل `datasource` في `schema.prisma`، الاتصال معرّف في `prisma.config.ts`)
- **PostgreSQL** (قاعدة بيانات محلية للتطوير)
- **NextAuth v4** (`next-auth@4.24.14`) + `@auth/prisma-adapter@2.x` مع Credentials Provider و`bcryptjs` لتجزئة كلمات المرور
- **Tailwind CSS v4**
- **Puppeteer** لتوليد PDF (تم استبدال `@react-pdf/renderer` بعد اكتشاف كسر في تشكيل الحروف العربية — راجع القسم 4)
- **moment-hijri** لتحويل التواريخ الهجرية
- **react-hot-toast** للإشعارات

## 3. هيكل المجلدات

```
prisma/
  schema.prisma          # كل النماذج والـ enums
  seed.ts                 # بيانات تجريبية: مستخدمَين، عملاء، قضايا، مسارات قضائية
  prisma.config.ts         # رابط الاتصال بقاعدة البيانات (خارج schema.prisma)

src/
  app/
    (auth)/login/          # صفحة تسجيل الدخول
    (dashboard)/            # كل الصفحات المحمية خلف الجلسة
      dashboard/            # لوحة التحكم الرئيسية + KPIs (تتكيّف حسب الدور)
      intake/[id]/ · intake/rejected/  # مسار استلام القضية + بنك الرفضات
      cases/[id]/            # قائمة القضايا + صفحة تفاصيل قضية (+ أطراف الدعوى)
      clients/[id]/          # قائمة العملاء + صفحة تفاصيل عميل
      calendar/               # تقويم شهري + جلسات قادمة + تنبيهات قوى
      sessions/               # جدول الجلسات المركزي (فلاتر + محضر + تأجيل)
      memos/[id]/ · memos/new/  # نظام اعتماد المذكرات (كتابة/مراجعة/اعتماد)
      tasks/[id]/ · tasks/       # نظام المهام (جدول/كانبان + تفاصيل + ملاحظات)
      templates/[key]/fill/    # قائمة النماذج + تعبئة حيّة + سجل النماذج
      reports/                 # تقارير أداء (KPIs، أداء المحامين، توزيع النوع/الصفة)
      invoices/                 # الفواتير والمصاريف (مسؤول النظام + محاسب)
      audit/                    # سجل التدقيق + تصدير CSV (مسؤول النظام فقط)
      settings/case-flows/       # إدارة المسارات القضائية (CRUD، مقيّد بالإدارة)
      settings/users/            # إدارة المستخدمين (مسؤول النظام فقط)
      settings/account/          # حسابي — تغيير كلمة المرور (كل المستخدمين)
      layout.tsx                 # فحص الجلسة + Sidebar/TopBar
    api/                         # كل الـ route handlers: intake/* (+delegate-assessment), cases, clients,
                                 # sessions, memos, tasks/*, invoices, expenses, users, audit,
                                 # case-flows, account/password، cases/[id]/parties...
  components/
    cases/                      # CasePipeline، SettlementPanel، ClosureBanner...
    layout/                     # Sidebar (مجموعات حسب الدور)، TopBar
    modals/                     # NewCase (+أطراف)، NewClient، NewIntake، NewTask، DuplicateWarning، CaseClosure...
    ui/                         # Skeleton، ErrorState، NumberField (حقل رقمي مقيّد)
  lib/
    auth.ts                     # إعداد NextAuth (يمنع/يُبطل الحسابات المعطّلة والقديمة)
    prisma.ts                   # عميل Prisma الموحّد
    rbac.ts                     # كل منطق الصلاحيات (انظر القسم 4)
    caseFlow.ts / caseClosure.ts  # منطق المسار القضائي وإغلاق القضايا
    memos.ts                      # حالات المذكرات + صلاحيات الكتابة/المراجعة
    tasks.ts                      # حالات/فئات/أولويات المهام + صلاحيات الإسناد والرؤية + حساب التأخّر
    intake.ts / conflictCheck.ts   # تسميات وصلاحيات الاستلام (+ تفويض التقييم) + فحص التعارض الآلي
    parties.ts                      # صفات أطراف الدعوى + الصفة المقابلة تلقائيًا
    validators.ts                    # تحقق أرقام سعودية (جوال 10 خانات + normalize/هوية Luhn/سجل) + قوة كلمة المرور
    duplicateCheck.ts                 # فحص تكرار الجوال/الهوية/الوكالة عبر العملاء/الطلبات/الأطراف
    dateUtils.ts                  # تحويلات هجري/ميلادي بأرقام إنجليزية
    formatNumber.ts                # تحويل أرقام عربية-هندية → إنجليزية + تنسيق عملة
    audit.ts                        # تسميات + شرط بحث سجل التدقيق (مشترك)
    dashboard-stats.ts / reports-stats.ts / case-filters.ts
    templates/definitions.ts        # تعريف النماذج الثمانية وحقولها
    pdf/, fonts/                     # مولّد Puppeteer وخطوط Amiri المضمّنة
  types/index.ts                     # أنواع مشتركة للواجهة (مثل أيقونات Sidebar)
```

## 4. القرارات المهمة المتخذة

### RBAC (الصلاحيات) — `src/lib/rbac.ts`
- **6 أدوار تطابق الهيكل الفعلي للمكتب**: `system_admin` (مسؤول النظام — صلاحيات كاملة)، `supervisor` (مشرف — مسؤول عن القضية ويحدد مسارها)، `lawyer` (محامٍ — يترافع ويعتمد المذكرات)، `researcher` (باحث قانوني — يكتب المذكرات)، `secretary`، `accountant`. (سابقًا كانت `partner`/`senior_lawyer` — استُبدلت بالكامل عبر الكود وقاعدة البيانات.)
- **الإدارة** (`isManagement` = `system_admin` فقط) ترى كل القضايا دون قيد. غيرهم يحتاج أن يكون محاميًا مسؤولًا، أو عضو فريق القضية، أو صاحب تفويض `allow` صريح.
- **DENY صريح يتفوّق دائمًا** حتى على عضوية الفريق — قرار متعمّد لحماية تعارض المصالح.
- منطق الرؤية مطبَّق مرتين بشكل متطابق: `canAccessCase` (تحقق كائن واحد) و`caseVisibilityWhere` (شرط Prisma عند الاستعلام) — يجب إبقاؤهما متزامنين عند أي تعديل مستقبلي. **نفس المبدأ ينطبق على العملاء**: `canAccessClient` + `clientVisibilityWhere` (عميل مرئي إذا كان له قضية مرئية للمستخدم؛ الإدارة والمحاسب يرون الكل).
- `accountant` لا يمكنه تعديل القضايا أو رفع المستندات، لكنه يدير الفواتير مع `system_admin` (`canManageInvoices`)، ويرى العملاء لأغراض الفوترة دون تفاصيل قضاياهم.
- صلاحيات حصرية لمسؤول النظام (`isSystemAdmin` / `canManageUsers` / `canViewAuditLog`): اعتماد/رفض الإغلاق وإعادة الفتح، إدارة المستخدمين (`/settings/users`)، وسجل التدقيق (`/audit`). إعدادات المسارات القضائية (`/settings/case-flows`) مقيّدة بـ `isManagement`.
- **أدوار فريق القضية** (`CaseTeamRole`): `supervisor` / `lawyer` / `researcher` (سابقًا `lead`/`assistant`). و`CASE_HANDLER_ROLES` = الأدوار التي تُسند إليها القضايا كمحامٍ مسؤول وتظهر في تقارير الأداء (`system_admin`, `supervisor`, `lawyer`).
- الشريط الجانبي (`Sidebar`) يبني مجموعاته من `role` عبر دوال `rbac` مباشرة، فلا تظهر روابط لا يملك الدور صلاحيتها.
- الحسابات تُعطَّل تعطيلًا ناعمًا (`User.isActive`) ولا تُحذف — الحساب المعطّل يُمنع من تسجيل الدخول في `auth.ts`، مع حماية بقاء **مسؤول نظام** نشط واحد على الأقل وعدم تعطيل الذات.

### رقم القضية الرسمي كمرجع أساسي — `src/lib/caseNumber.ts` + `Case.displayNumber`
- **الأولوية في العرض**: `courtCaseNumber` (رقم المحكمة الرسمي) ← رقم منصة التسوية (`amicableSettlement.requestNumber` قوى/تراضي) ← `internalNumber` (الرقم الداخلي، احتياطي). المنطق موحّد في `getPrimaryCaseNumber`/`getAllCaseNumbers`/`computeDisplayNumber` (ملف نقيّ يُستورد في الواجهة أيضًا).
- **`courtCaseNumber` بلا validation صارم**: يقبل أي تنسيق (أرقام/حروف/رموز/شرطات) لتعدّد أشكال أرقام المحاكم — مثال: `4568/ي-1447` أو `44567890`.
- **`Case.displayNumber` حقل مُحسَّب مسبقًا** (نسخة مخزّنة من الرقم المفضّل) للاستعلام/العرض السريع. **يُزامَن عند الكتابة فقط** عبر `syncCaseDisplayNumber` في `src/lib/caseNumber.server.ts` (يقبل عميل Prisma أو `tx`): يُضبط = `internalNumber` عند إنشاء القضية (POST `/api/cases` + تفعيل الاستلام)، ويُعاد حسابه عند تعديل `courtCaseNumber` (PATCH `/api/cases/[id]`) أو إضافة/تعديل رقم قوى/تراضي (POST/PATCH `amicable-settlement`).
- **⚠️ لا يوجد Prisma middleware**: Prisma 7 أزال `prisma.$use` (تحقّقنا: `undefined`؛ `$extends` فقط المتاح). لذلك المزامنة **صريحة عند مواضع الكتابة** لا عبر middleware — وهو أيضًا أنظف وأكثر حتمية ويتجنّب استعلامًا إضافيًا في كل قراءة.
- **البحث بأي رقم**: `buildCasesWhere` (قائمة القضايا) يبحث في `internalNumber` + `courtCaseNumber` + `amicableSettlement.requestNumber` + العنوان + اسم العميل — فالمحامي يجد القضية بأي رقم يعرفه.
- **العرض**: مكوّن `CaseNumberDisplay` (`inline`/`card`/`detailed`) — شارة لونية حسب المصدر (محكمة أخضر · قوى/تراضي أزرق · داخلي رمادي)، أرقام إنجليزية دائمًا (`toEnglishDigits`, `dir="ltr"`). مستخدم في قائمة القضايا (عمود «الرقم») وصفحة التفاصيل (رقم رسمي بارز أعلى العنوان + بطاقة «أرقام القضية» بالجدول التفصيلي + بانر «لم يُضَف رقم المحكمة» + `EditCourtNumberModal`). النماذج: مفتاح autofill جديد `primaryCaseNumber` لحقول «رقم القضية» العامة (بينما «رقم ملف العميل» يبقى `internalNumber` و«رقم الدعوى» في تقرير الجلسة يبقى `courtCaseNumber`).
- **seed**: توزيع يغطي كل المصادر — قضيتان برقم محكمة (`4568/ي-1447`, `10247/ب-1446`)، قضيتان عماليتان برقم قوى فقط، وقضيتان بالرقم الداخلي فقط (لم تُرفع بعد). حلقة backfill في نهاية `seed.ts` تضبط `displayNumber` لكل القضايا بشكل idempotent.

### نظام التعريفات الموحّد (Tooltips) — `src/lib/fieldDefinitions.ts` + `src/components/ui/`
- **قاموس مركزي** `fieldDefinitions` (`as const satisfies`): كل مصطلح قانوني غامض له `{ label, tooltip, example?, category }`. الفئات في `FIELD_CATEGORY_LABELS_AR`. **لإضافة تعريف**: أضِف مفتاحًا واحدًا هنا — يتوفّر تلقائيًا في المكوّن وفي `/glossary`.
- **الاستخدام في النماذج**: بدل `<label>الحقل *</label>` استخدم `<DefinedField definitionKey="opposing_party" required htmlFor="fieldId" />` ثم `<input id="fieldId" .../>`. `DefinedField` يقرأ من القاموس ويمرّر إلى `FieldTooltip`.
- **`FieldTooltip`** (`components/ui/FieldTooltip.tsx`): تسمية + أيقونة (ⓘ) تُظهر التوضيح **بالتحويم (desktop) وبالنقر (mobile)** — بلا مكتبة خارجية (لا Radix/shadcn في المشروع)، مع إغلاق عند النقر خارجها أو Escape، و`aria-label`/`role="tooltip"` للوصول.
- **مطبَّق في**: نموذج الاستلام (`opposing_party`, `dispute_summary`)، القضية الجديدة (`case_type`, `agency_scope`, `client_party_role`)، إغلاق القضية (`case_outcome`)، المهمة (`task_priority`)، المذكرة (`legal_memo`). بقية المصطلحات (أنواع الجلسات/النماذج...) مغطّاة في صفحة القاموس.
- **صفحة `/glossary`**: مرجع كامل (بحث + تصفية بالفئات + بطاقات عنوان/تعريف/مثال)، رابط في الشريط الجانبي «📖 قاموس المصطلحات».
- **رسائل خطأ تعليمية** — `VALIDATION_MESSAGES` في `validators.ts` (جوال/هوية/سجل/وكالة تشرح القاعدة + مثال، بدل «رقم غير صحيح»)، مستخدمة في `NumberField` وكل مسارات الأرقام (cases/clients/intake).

### نظام الإشعارات — `src/lib/notifications/*`
- **قرار المعمارية: Polling كل 30 ثانية (وليس SSE).** السبب: 5–10 مستخدمين فقط؛ نقطة `unread-count` خفيفة جدًا؛ Next.js على Docker/Hetzner خلف reverse proxy يعمل مع polling بلا إعداد، بينما SSE يتطلب تعطيل buffering وضبط timeouts وإدارة إعادة الاتصال (ومع أكثر من instance يحتاج Redis pub/sub)؛ الإشعارات القانونية غير حرجة بالثانية. **الترقية لاحقًا سهلة** لأن طبقة `sendNotification` + محوّلات القنوات تعزل آلية التوصيل عن المُطلقات.
- **قناة-جاهزة (Channel-Ready)**: `ChannelAdapter` لكل قناة في `channels/` — `in_app` (يرجع delivered فورًا، الحفظ في DB هو التوصيل)، و`email`/`sms`/`whatsapp` **stubs** ترجع `pending`. `getAdapter(channel)` يوزّع. **إضافة قناة لا تتطلب تعديل أي مُطلِق.**
- **الدالة الموحّدة** `sendNotification(payload)` (و`sendBulkNotification`): تقرأ تفضيلات المستخدم للنوع → تحفظ الإشعار في DB → ترسل عبر كل قناة → تحدّث `deliveryLog`. أغلفة آمنة `notify`/`notifyBulk` **لا تُفشل العملية الأساسية أبدًا** (try/catch) وتُستدعى بعد نجاح الجوهر في مسارات الـ API. **⚠️ قاعدة: بعد أي API فيه حدث يهمّ مستخدمًا، أضِف `notify`/`notifyBulk` بعد كتلة الـ audit.**
- **المُطلقات (Triggers) المدمجة**:
  - الاستلام: إنشاء طلب → `intake_new` (لمسؤولي النظام)؛ تعارض مؤكد (عند الإنشاء أو `conflict-check`) → `intake_conflict_detected` (urgent)؛ `delegate-assessment` → `intake_assessment_delegated` (للمُفوَّض, high)؛ `assessment` → `intake_pending_decision` (لمسؤولي النظام+المشرفين)؛ `decision` → `intake_accepted`/`intake_rejected` (للمستلِم).
  - المذكرات: `submit` → `memo_pending_review` (لمراجعي القضية: المسؤول+مشرفو/محامو الفريق, high)؛ `review` → `memo_approved`/`memo_changes_requested` (للكاتب).
  - القضايا: إنشاء → `case_assigned` (للمحامي المسؤول)؛ `closure` POST → `case_closure_requested` (لمسؤولي النظام)؛ `closure` PATCH → `case_closure_approved`/`case_closure_rejected` (للطالب)؛ `reopen` → `case_reopened` (للفريق)؛ PATCH مع `courtCaseNumber` جديد → `case_number_added` (للفريق).
  - الجلسات: `sessions` POST → `session_scheduled` (لفريق القضية).
  - المهام: إنشاء → `task_assigned` (للمسند إليه إن ≠ المُنشئ)؛ `status=completed` → `task_completed` (للمُسنِد)؛ `comments` → `task_comment_added` (للطرف الآخر).
  - المُطلقات تستثني الفاعل نفسه من المستقبلين (لا إشعار ذاتي).
- **المهام المجدولة (Time-based)** — `scheduler.ts` / `checkTimeSensitiveNotifications()`: تذكيرات الجلسات (24 ساعة `session_reminder_day` high، ساعة `session_reminder_hour` urgent)؛ الوكالات (30/7 أيام/منتهية)؛ مهل التسوية (7/2 أيام)؛ المهام (تقترب/متأخرة)؛ الفواتير المتأخرة. **منع التكرار** عبر `notifyOnce` (لا يُعاد الإرسال ضمن نافذة dedup لكل مورد). تُشغَّل عبر **GET `/api/cron/notifications`** المحمي بترويسة `x-cron-secret` (= `CRON_SECRET`) — أو جلسة مسؤول نظام في التطوير. زر يدوي في `/dev/trigger-reminders` (مسؤول نظام + تطوير فقط).
- **API**: `GET /api/notifications` (فلاتر: status/type/priority/days + ترقيم 20)، `GET /unread-count` (سريع للـ polling)، `POST /[id]/read`، `POST /read-all`، `DELETE /[id]`، `POST /delete-old` (المقروء +30 يوم)، `GET|PATCH /preferences`. كل العمليات مقيّدة بملكية المستخدم (`recipientId`).
- **الواجهة**: `NotificationBell` (TopBar) — polling كل 30ث لعدّاد غير المقروء + dropdown بآخر 10 + toast للعاجل/المهم الجديد (react-hot-toast) + تحديد الكل مقروء. صفحة `/notifications` (فلاتر + ترقيم + تحديد/حذف + تجميع حسب اليوم). صفحة `/settings/notifications` (مصفوفة نوع×قناة، in_app مفعّل والبقية «قريبًا»). البيانات الوصفية (تسمية/وصف/أيقونة/فئة/أولوية لكل نوع) في `meta.ts`.
- **تفعيل البريد لاحقًا**: (1) `EMAIL_ENABLED=true` + `RESEND_API_KEY` + `EMAIL_FROM` في البيئة؛ (2) استبدال جسم `emailAdapter.send` باستدعاء المزوّد؛ (3) المستخدم يفعّل قناة `email` من `/settings/notifications`. **لا حاجة لتعديل أي مُطلِق.** نفس النمط لـ SMS/واتساب.
- **التفضيلات الافتراضية**: كل نوع → `in_app` (`defaults.ts`). `seed.ts` يبذر تفضيلات لكل مستخدم موجود (userId×type) + 20 إشعارًا تجريبيًا (8 غير مقروءة: 2 عاجل/3 مهم/3 عادي، 12 مقروءة قديمة).

### نظام اعتماد المذكرات (LegalMemo) — `src/lib/memos.ts`
- سير عمل فعلي: المشرف+المحامي يحددون التوجه ← **الباحث** يكتب المذكرة (مسودة) ← يرسلها (`submitted`) ← **المحامي** يراجع فيعتمد (`approved`) أو يطلب تعديلات (`changes_requested`، والملاحظات إلزامية) ← بعد الاعتماد يحدّدها المحامي `submitted_to_court`.
- قيود الحالة مفروضة في الـ API: الباحث لا يعدّل إلا وهي `draft` أو `changes_requested` (`canEditMemo`)؛ المراجعة (`canReviewMemo`) للمحامي فقط والمذكرة `submitted`؛ التقديم للمحكمة فقط بعد `approved`. كل مراجعة تُسجَّل في `MemoReview` مع ملاحظات المحامي وتاريخها.
- `memoVisibilityWhere` = مذكرات القضايا المرئية للمستخدم فق. لوحة التحكم وصفحة المذكرات تعرضان بطاقات ملخص مختلفة حسب الدور (باحث: قيد الكتابة/تعديلات مطلوبة/معتمدة؛ محامي: بانتظار مراجعتي/معتمدة).

### أطراف الدعوى وصفة الموكل — `src/lib/parties.ts` + `prisma/schema.prisma`
- `PartyRole` وُسِّع إلى 7 صفات (مدعي/مدعى عليه/مستأنف/مستأنف ضده/مقدم طلب/مدعى عليه إداري/طرف ثالث). كل قضية لها **`clientPartyRole`** (صفة موكّلنا، للاستعلام السريع) و`CaseParty` صار يحمل `isOurClient` (**الحقل الأهم** — يميّز موكّلنا عن الخصم)، `identityNumber`, `phone`, `address`, `opposingCounsel`, `notes`.
- قرارات: **عدد الأطراف مفتوح** (موكّل + طرف مقابل واحد على الأقل، وأطراف إضافية اختيارية)؛ **موكّل واحد إلزامي لكل قضية ولا يُحذف** (`cases/[id]/parties` DELETE يرفض حذف `isOurClient`)؛ صفة الطرف المقابل **تُحسب تلقائيًا** عكس صفة الموكل (`OPPOSING_ROLE`).
- العرض: بطاقة "أطراف الدعوى" في صفحة القضية بعمودين متقابلين (موكّلنا أخضر `#3F7A5C` + شارة "موكّلنا ✓"، الطرف المقابل رمادي + محاميه)، وعنوان "س (مدعي) ضد ص (مدعى عليه)"، وعمود "صفتنا" في قائمة القضايا، وتقرير "توزيع القضايا حسب صفتنا" (معدل الكسب كمدّعين مقابل مدّعى عليهم). النماذج تُعبّئ المدعي/المدعى عليه وصفة الموضوع تلقائيًا من الأطراف.

### مسار استلام القضية (Intake) — `src/lib/intake.ts` + `src/lib/conflictCheck.ts`
- **قرار جوهري: القضية لا تُنشأ مباشرة.** يمر الطلب أولًا بكيان مستقل `IntakeRequest` عبر مسار: **استقبال ← فحص تعارض ← تقييم ← عقد أتعاب ← تفعيل قضية**. عند التفعيل فقط يُنشأ `Case` (+ `Client` أو ربط بموجود + `CaseParty` للطرفين + نقل `IntakeDocument`→`Document`)، ويُربط بـ`IntakeRequest.caseId`.
- **فحص تعارض المصالح آلي** (`conflictCheck.ts`) عند الإنشاء وعند الطلب: الطرف المقابل عميل حالي بقضية نشطة → **مؤكد**؛ كان موكّلنا سابقًا → **مؤكد**؛ يظهر كطرف مقابل في قضية نشطة → **محتمل**؛ وإلا نظيف. يُخزَّن في `conflictResult`/`conflictNotes`.
- صلاحيات: **إنشاء الطلب لكل الموظفين**؛ رؤية الطلبات لمسؤول النظام/المشرف/المستلم **والمُفوَّض إليه** (`intakeVisibilityWhere`)؛ **بنك الرفضات** (`/intake/rejected`) لمسؤول النظام والمشرف. ترقيم `INT-YYYY-NNNN` بأسلوب أكبر رقم (يتحمّل الفجوات).
- **تفويض التقييم**: بدل حصر التقييم بمسؤول النظام، صار **مسؤول النظام أو المشرف يقيّم بنفسه أو يفوّض التقييم** لأي موظف (عدا السكرتارية والمحاسب) عبر `IntakeRequest.assessmentDelegatedTo*`. المُفوَّض إليه **يعبّئ ويحفظ التقييم فقط** (`canAssessIntake(user, intake)` = مسؤول نظام/مشرف/مُفوَّض إليه) لكن **القرار (قبول/رفض) والتفعيل يبقيان لمسؤول النظام والمشرف** (`canDecideIntake`/`canActivateIntake` = مسؤول نظام + مشرف). API: `intake/[id]/delegate-assessment` (POST تفويض / DELETE إلغاء — للمُفوِّض أو مسؤول النظام).
- **النماذج والمستندات في مرحلة الاستلام**: صفحة الطلب تعرض «النماذج المطلوبة قبل الاعتماد» (النماذج غير المرتبطة بقضية: `linkedTo` = `none`/`case_optional` عبر `isIntakeEligibleTemplate`) تُعبّأ بـ`FilledTemplate.intakeId` (تعبئة تلقائية من بيانات العميل الأولية)، إضافة لقسم المستندات الأولية. **عند التفعيل تنتقل تلقائيًا للقضية**: `filledTemplate.updateMany({intakeId} → {caseId, intakeId:null})` والمستندات → `Document` (ضمن معاملة التفعيل).

### نظام المهام (Task) — `src/lib/tasks.ts`
- كيان مهام مستقل: **قد يرتبط بقضية أو طلب استلام أو يكون مستقلًا تمامًا** (`caseId?`/`intakeId?` كلاهما اختياري). حقول: `taskNumber` (`TSK-YYYY-NNNN`)، `category` (8 فئات)، `priority` (4)، `status` (5)، `assignedTo`/`assignedBy`، `dueDate`/`startedAt`/`completedAt`، `completionNote`، و`TaskComment` (سجل ملاحظات).
- **حالة «متأخرة» محسوبة تلقائيًا** (`displayTaskStatus`/`isTaskOverdue`): لا تُخزَّن، بل تُشتقّ عند العرض (معلقة/قيد تنفيذ + استحقاق فائت). عرضان في `/tasks`: **جدول** و**كانبان** (أعمدة: معلقة/قيد التنفيذ/منجزة، والكانبان يعتمد الحالة الفعلية `rawStatus`).
- **صلاحيات** (`tasks.ts`): الرؤية = المسند إليه أو المُنشئ (والإدارة/المشرف: الكل، `taskVisibilityWhere`). الإسناد (`canAssignTaskTo`): مسؤول النظام/المشرف لأي موظف؛ الجميع لنفسه؛ المحامي/الباحث لباحث **يشاركه فريق قضية** (فحص عبر عضوية الفريق). تغيير الحالة للمسند إليه (تقدّم) أو المُنشئ (إلغاء)؛ التعديل/الإلغاء الناعم للمُنشئ أو الإدارة (`canManageTask`). عند الإنجاز `completionNote` إلزامي.
- API: `tasks` (GET/POST مع ترقيم تلقائي)، `tasks/[id]` (GET/PATCH/DELETE ناعم)، `tasks/[id]/status`، `tasks/[id]/comments`. كل عملية تُسجَّل في `audit_log`. مكوّن `NewTaskModal` مشترك (يُستدعى من صفحة المهام والقضية والطلب مع `presetCaseId`/`presetIntakeId`). أقسام «المهام» في صفحة القضية والطلب، وبطاقات مهام في لوحة التحكم. نموذج التقييم في الاستلام فيه زر «إنشاء مهمة».

### قرارات الأدوار والهيكل
- **مسؤول النظام (`system_admin`) هو مركز الحوكمة**: يعتمد/يرفض إغلاق القضايا وإعادة فتحها، **ويقيّم ويقرّر ويفعّل طلبات الاستلام الجديدة**، ويدير المستخدمين وسجل التدقيق.
- **المشرف (`supervisor`) اختياري** في فريق القضية (ليس كل قضية تحتاج مشرفًا) — لكن **يجب محامٍ مسؤول واحد** (`responsibleLawyer`، يُضاف تلقائيًا لفريق القضية بدور `lawyer`). أدوار الفريق: `supervisor`/`lawyer`/`researcher`.

### المسارات القضائية (CaseFlowStage) — `src/lib/caseFlow.ts` + `prisma/schema.prisma`
- بدل تصلّب منطق "3-4 مراحل" في الكود، المسار الآن **بيانات في جدول `case_flow_stages`** (نوع القضية، الترتيب، الاسم، إلزامي/اختياري، الجهة، رابط المنصة). يتيح هذا لمكتب المحاماة تعديل المسار عند تغيّر الأنظمة دون تدخل مبرمج.
- 7 مجموعات مسارات مزروعة (27 مرحلة): عام/تجاري/أحوال شخصية/تحصيل ديون تتشارك نفس المسار (صلح اختياري ← ابتدائية إلزامية ← استئناف اختياري ← عليا اختياري)، عمالي (تسوية إلزامية عبر قوى)، إداري (ديوان المظالم، تظلّم اختياري إلا في القرارات الوظيفية)، لجان (3 مراحل فقط). الجنائي والتحكيم بلا مرحلة تسوية — يذهبان للمحكمة مباشرة.
- **تمييز مهم بين منصتين حكوميتين مختلفتين لا يجوز الخلط بينهما**:
  - **قوى (Qiwa)** — وزارة الموارد البشرية، للتسوية الودية العمالية، **إلزامية** قبل رفع الدعوى.
  - **تراضي (Taradhi)** — وزارة العدل، للصلح في القضايا التجارية/المدنية/الأحوال الشخصية، **اختيارية**.
  - `canProceedToCourt()` يمنع رفع الدعوى فقط إذا كانت أول مرحلة في مسار نوع القضية `isMandatory: true` ولم تكتمل التسوية بعد.
- `CasePipeline` و`SettlementPanel` مكوّنان عامّان يُبنيان ديناميكيًا من `CaseFlowStage[]` بدل شرط `caseType === "labor"` القديم، مع سقوط احتياطي (fallback) لعرض 3 خطوات "استلام ← محكمة مباشرة ← حكم" لأنواع القضايا بلا مراحل مُعرَّفة.

### التاريخ الهجري والأرقام الإنجليزية — `src/lib/dateUtils.ts`, `src/lib/formatNumber.ts`
- الواجهة عربية RTL لكن **كل الأرقام إنجليزية دائمًا** (وليست ٠١٢٣٤٥٦٧٨٩ عربية-هندية) — قرار ثابت غير قابل للتفاوض بناءً على طلب المستخدم، مطبَّق عبر `moment.locale("en")` + `toEnglishDigits()` + `font-feature-settings: "lnum"` في `globals.css`.
- كل تاريخ في الواجهة يُعرض بصيغة مزدوجة (هجري + ميلادي) عبر `formatDualDate`/`formatDualDateTime`، وتُحفظ `hijriDate` تلقائيًا عند إنشاء/تعديل أي جلسة.
- تم حذف تطبيق هجري قديم (`lib/templates/hijri.ts` المبني على `Intl`) لأنه كان يُخرج أرقامًا عربية-هندية — `moment-hijri` هو المصدر الوحيد المعتمد الآن.

### تخزين المستندات (بدون R2 حاليًا)
- الرفع الحالي (`src/app/api/documents/upload/route.ts`) يكتب الملفات مباشرة على القرص المحلي تحت `public/uploads/<caseId>/`، وليس على أي خدمة تخزين سحابي (لا يوجد Cloudflare R2 أو S3 مُفعّل بعد).
- هذا **قرار مؤقت مقصود** لتسريع التطوير المحلي؛ تم ترك تعليق `TODO` صريح في الكود عند نقطة الكتابة يشير إلى استبدالها برفع سحابي (R2 أو ما يعادله) عند الانتقال للإنتاج. يجب عدم الاعتماد على `public/uploads` في بيئة إنتاج متعددة الخوادم لأنه تخزين محلي غير مشترك.

### مولّد PDF: Puppeteer بدل @react-pdf/renderer
- `@react-pdf/renderer` جُرِّب أولًا لكنه أنتج نصًا عربيًا مكسورًا (حروف منفصلة، bidi خاطئ) لعدم وجود محرك تشكيل نصوص (text shaping) حقيقي — أُزيل من المشروع.
- الحل المعتمد: بناء HTML كامل مع خط Amiri مضمّن كـ base64 عبر `@font-face`، ثم `puppeteer.page.setContent()` و`page.pdf()` بأعلام `--no-sandbox` وانتظار `document.fonts.ready`. تم التحقق بصريًا من الناتج (تشكيل عربي متصل، RTL سليم، أرقام إنجليزية، ترويسة خضراء `#16342A`).

### الأخطاء الأربعة الحرجة (مُختبَرة عبر HTTP)
- **توليد PDF (عربي مكسور)**: `@react-pdf/renderer` كان يُخرج حروفًا عربية منفصلة وbidi خاطئًا لغياب محرك text shaping — استُبدل بـ Puppeteer مع خط Amiri مضمّن base64 عبر `@font-face` (التفاصيل الكاملة في «مولّد PDF: Puppeteer» أعلاه).
- **حالة التسوية (الأخطر — قانوني)**: كانت حالة القضية تُزامَن فقط عند `settled`، فتبقى "تمت التسوية" بعد تصحيح النتيجة إلى `failed`. الآن `amicable-settlement` PATCH يزامن الحالة لكل النتائج: `settled`→`settled_amicably`، `failed`→`open` (رفع الدعوى)، `pending`→`amicable_settlement`.
- **تغيير كلمة المرور (أمني)**: `POST /api/account/password` بتحقق من الحالية + قوة الجديدة + `bcrypt.hash` + `update` + تدقيق، وصفحة `/settings/account`؛ وأُصلحت إعادة تعيين المشرف لترفض الكلمات الضعيفة بدل تجاهلها بصمت.
- **تحقق الأرقام** (`src/lib/validators.ts`): جوال سعودي، هوية/إقامة بخوارزمية Luhn، سجل تجاري، رقم وكالة، وقوة كلمة المرور (8+/كبير/صغير/رقم) — مُطبَّق خادميًّا في واجهات العملاء والقضايا والمستخدمين والاستلام.
### إصلاحات أخرى
- **تحصين الجلسات**: `auth.ts` يتحقق على كل طلب من وجود المستخدم ونشاطه (يُبطل الجلسات القديمة بعد `migrate reset` أو تعطيل حساب فورًا) — كان سببًا خفيًّا لأعطال "تعذّر الإضافة/توليد PDF". كما أُصلح توليد `internalNumber` ليعتمد أكبر رقم لا العدّ (يتحمّل الفجوات).

### نظام فحص التكرار والتحقق الصارم — `src/lib/validators.ts` + `src/lib/duplicateCheck.ts`
- **الجوال**: القاعدة صارت **10 خانات تبدأ بـ 05 بالضبط** (`isValidSaudiPhone` عبر `normalizeSaudiPhone` الذي يحوّل `+9665X`/`9665X` → `05X`). كل الأرقام تُوحَّد (`normalize`) قبل الحفظ والمقارنة لمنع التكرار المموّه. `saudiPhoneError` يعطي رسالة فورية للواجهة.
- **الهوية/السجل**: 10 أرقام (الهوية بخوارزمية Luhn، السجل 10 أرقام). في الاستلام يُقبل 10 أرقام. **تفرّد `Client.nationalIdOrCr` مضمون عبر `@unique`** (خط دفاع أخير على مستوى القاعدة).
- **فحص التكرار** (`duplicateCheck.ts`): `checkPhoneDuplicate`/`checkIdentityDuplicate`/`checkAgencyDuplicate` تبحث عبر **العملاء + طلبات الاستلام (غير المرفوضة/الملغاة وغير المُفعَّلة) + أطراف القضايا**. تُستثنى الطلبات المُفعَّلة (`caseId != null`) لأنها صارت العميل نفسه (تكرار مشروع لا تحذير).
- **آلية التحذير القابل للتجاوز**: عند اكتشاف تكرار ترجع الـ API **409** بجسم `{ error:"duplicate_detected", duplicateInfo:{ type, existingIn:[...] } }`. الواجهة تعرض `DuplicateWarningModal` بثلاثة خيارات: «استخدام السجل الموجود» / «متابعة الحفظ» (يعيد الإرسال بـ `force:true` ويُسجَّل في التدقيق) / «إلغاء». **لا نضع فهرس فريد على الجوال** لأنه يناقض ميزة الحفظ المتعمّد؛ بدلًا منه `@@index([phone])` غير فريد للأداء + الفحص التطبيقي.
- **مطبَّق في**: POST `/api/intake`، POST/PATCH `/api/clients`، POST `/api/cases` (عميل جديد + وكالة)، POST/PATCH `/api/cases/[id]/parties`، وتوحيد جوال `/api/users`. نقطة فحص حيّة: GET `/api/duplicate-check`.
- **الواجهة**: `NumberField` (منع الأحرف غير الرقمية + `maxLength` + `inputMode=numeric` + رسالة فورية) في كل النماذج؛ **بانر حيّ في الاستلام**: بعد 10 خانات يفحص الرقم (debounced) ويعرض «هذا الرقم مسجّل لـ ...» مع زر «استخدام بياناته» (تعبئة تلقائية للاسم والهوية). أداة `/dev/duplicate-check` (مسؤول النظام) تسرد كل التكرارات الحالية للتنظيف.

## 5. ما تم إنجازه حتى الآن

- المصادقة والصلاحيات (NextAuth + RBAC كامل بـ**6 أدوار** فعلية، مع تحصين الجلسات).
- لوحة التحكم بمؤشرات أداء حيّة **تتكيّف حسب الدور** (باحث: مذكراته؛ محامٍ: مراجعاته؛ مسؤول النظام: طلبات الاستلام/الإغلاق + تنبيهات التعارض).
- إدارة القضايا الكاملة: قائمة مفلترة/مرقّمة، صفحة تفاصيل، Modal إنشاء بثلاثة أقسام (عميل/وكالة/قضية)، مسار قضائي ديناميكي.
- إدارة العملاء (قائمة + تفاصيل + وكالات مرتبطة بتنبيه انتهاء صلاحية).
- التقويم الشهري مع قائمة الجلسات القادمة وتنبيهات مهلة قوى.
- النماذج القانونية: 8 نماذج معرّفة، تعبئة حيّة مع تعبئة تلقائية من بيانات القضية، توليد PDF بالهوية الخضراء، سجل نماذج معبأة.
- التقارير: قضايا مغلقة، معدل الكسب، تسويات ودية، فواتير مستحقة، أداء المحامين، توزيع أنواع القضايا.
- نظام التسوية الودية المصحَّح (قوى/تراضي) بمنطق منع/سماح صحيح حسب النوع.
- المسارات القضائية الأربعة القابلة للتعديل من واجهة إعدادات مخصّصة، مبنية بالكامل على جدول `case_flow_stages`.
- التاريخ الهجري والأرقام الإنجليزية مطبّقان في كل الصفحات (تحقق فعلي عبر HTTP أن لا رقم عربي-هندي واحد يظهر في أي صفحة).
- **نظام إغلاق القضايا بالموافقة والنتيجة** (`CaseClosureRequest`, `CaseReopenLog`): المحامي المسؤول يطلب الإغلاق (نتيجة + سبب + ملخص إلزامية) فتصبح الحالة `pending_closure`؛ **مسؤول النظام فقط** (`isSystemAdmin`) يعتمد (→ `closed` + حفظ `outcome` + `closedDate`) أو يرفض (→ `in_progress` + سبب الرفض)؛ إعادة الفتح (مسؤول النظام فقط، بسبب إلزامي) تُسجَّل في `CaseReopenLog` وتحذف طلب الإغلاق السابق. مُختبَر عبر HTTP بالكامل. المكوّنات: `CaseClosureModal`, `ClosureRequestBanner`, `ClosedCaseBanner`, `ReasonPromptModal`.
- إصلاح "معدل الكسب" في التقارير ليعتمد فعليًا على `outcome` (كسب كامل + جزئي ÷ إجمالي المغلقة)، مع توزيع نتائج القضايا وعدّاد قضايا الصلح، وبطاقة "طلبات إغلاق بانتظار اعتمادك" لمسؤول النظام في لوحة التحكم.
- **إصلاح ثغرة صلاحيات العملاء**: قائمة العملاء وصفحة تفاصيل العميل وواجهات API صارت تطبّق `clientVisibilityWhere`/`canAccessClient` — الشريك/المحامي الأول يرى الكل، المحاسب يرى الكل لأغراض الفوترة (دون تفاصيل القضايا)، وبقية الأدوار ترى فقط عملاء القضايا التي هم أعضاء في فريقها. تحقق فعلي: الشريك يرى 4 عملاء والمحامي 2.
- **صفحة الجلسات المركزية** (`/sessions`): جدول كل الجلسات مفلترًا بالصلاحيات (بحث/نوع/حالة/فترة/محامي)، مع تسجيل محضر الجلسة (`SessionMinutes` — يسجّلها كـ«انعقدت»)، تأجيل بموعد جديد، ورابط تقرير الجلسة. API: `sessions/[id]/minutes`.
- **سجل التدقيق** (`/audit`، لمسؤول النظام فقط): جدول مفلتر (مستخدم/إجراء/نوع مورد/فترة) بترقيم 50/صفحة وتصدير CSV (بترميز UTF-8 وBOM). API: `api/audit` + `api/audit/export`.
- **إدارة المستخدمين** (`/settings/users`، لمسؤول النظام فقط): إضافة (bcrypt + كلمة مرور مؤقتة تُولَّد وتُعرض مرة)، تعديل (اسم/جوال/دور/إعادة تعيين كلمة مرور)، وتعطيل ناعم (`isActive`) مع حماية: لا تعطيل الذات، بقاء **مسؤول نظام** نشط واحد على الأقل، ونقل القضايا النشطة عند تعطيل محامٍ. الحساب المعطّل يُمنع من تسجيل الدخول.
- **الفواتير والمصاريف** (`/invoices`، لمسؤول النظام والمحاسب): تبويبان — فواتير (ضريبة 15% تلقائية قابلة للتعديل، بطاقات ملخص مستحق/مدفوع/متأخر، "تحديد كمدفوعة") ومصاريف (تسجيل لكل قضية + إجمالي كل قضية). API: `invoices`, `invoices/[id]`, `expenses`.
- **أطراف الدعوى وصفة الموكل** (انظر القسم 4): `PartyRole` بـ7 صفات، `CaseParty.isOurClient`، `Case.clientPartyRole`، بطاقتان متقابلتان (موكّلنا أخضر/الخصم رمادي)، عمود "صفتنا"، تقرير التوزيع حسب الصفة، وتعبئة تلقائية للنماذج. API: `cases/[id]/parties` (CRUD مع منع حذف موكّلنا). مُختبَر عبر HTTP.
- **مسار استلام القضية الكامل** (`IntakeRequest`, `IntakeDocument`, `IntakeNote`): صفحات `/intake` (قائمة + 4 بطاقات ملخص + فلاتر)، `/intake/[id]` (شريط تقدم 5 مراحل + كل الأقسام + مودالات رفض/تفعيل)، `/intake/rejected` (بنك الرفضات)؛ فحص تعارض آلي (`conflictCheck.ts`)؛ 7 مسارات API (list/create مع فحص تلقائي، detail/patch، conflict-check، assessment، decision، activate، notes، documents)؛ التفعيل يُنشئ Case+Client+طرفين وينقل المستندات. مُختبَر عبر HTTP: دورة كاملة + تعارض مؤكد تلقائي + حواجز الأدوار.
- **نظام المهام الكامل** (`Task`, `TaskComment`): صفحة `/tasks` (جدول + كانبان + 4 بطاقات ملخص + فلاتر بحث/حالة/أولوية/فئة/مسند إليه/قضية)، `/tasks/[id]` (بدء/إنهاء/تعديل/إلغاء + ملاحظات زمنية + مودال إنهاء بملخص إلزامي)، 4 مسارات API، ربط اختياري بقضية/طلب استلام، وأقسام مهام في صفحتي القضية والطلب + بطاقات في لوحة التحكم + رابط في الشريط الجانبي. مُختبَر عبر HTTP: إنشاء مستقل/مربوط، ترقيم `TSK-YYYY-NNNN`، دورة الحالة (بدء ← إنهاء بملاحظة إلزامية)، وحواجز الإسناد (محامٍ لا يسند لباحث خارج فريقه → 403).
- **تحسينات الاستلام** (انظر القسم 4): تفويض التقييم (تفويض/إلغاء + المُفوَّض يقيّم دون قرار) والنماذج/المستندات في مرحلة الاستلام مع انتقالها التلقائي للقضية عند التفعيل. مُختبَر عبر HTTP: دورة كاملة (إنشاء ← تفويض لسلطان ← تقييم بسلطان ← رفض قرار سلطان 403 ← قبول وتفعيل بأنس ← تأكيد انتقال النموذج للقضية).
- **نظام فحص التكرار والتحقق الصارم** (انظر القسم 4): توحيد الجوال لـ10 خانات، فحص تكرار الجوال/الهوية/الوكالة عبر العملاء/الطلبات/الأطراف مع تحذير 409 قابل للتجاوز (`force`)، حقول رقمية مقيّدة في الواجهة، بانر استلام حيّ، وأداة `/dev/duplicate-check`. مُختبَر عبر HTTP.
- **إصلاح 4 أخطاء حرجة** (انظر القسم 4): حالة التسوية المعكوسة، تغيير كلمة المرور الذاتي، توليد PDF، وتحقق الأرقام (`validators.ts`).
- الشريط الجانبي صار يبني مجموعاته حسب الدور: الاستلام / إدارة القضايا (+الجلسات +المذكرات) / الأدوات / المالية (مسؤول نظام+محاسب) / الإدارة (مسؤول نظام) / الحساب (الجميع).
- **تحديث الأدوار للهيكل الفعلي** (6 أدوار: `system_admin`/`supervisor`/`lawyer`/`researcher`/`secretary`/`accountant`) واستبدال `partner`/`senior_lawyer` في كل الكود وقاعدة البيانات. `seed.ts` يزرع 7 مستخدمين فعليين (`@qudum.sa`، كلمة المرور `Test1234`): **أنس الغامدي، عبدالرحمن الزهراني** (مسؤولا نظام)، **لمياء البردي، سحر السالمي، عمر الثمالي** (محامون)، **سلطان النمري، يزيد الغامدي** (باحثان) — بقضايا موزّعة على المحامين الثلاثة وباحثَين مسندَين للفرق.
- **نظام اعتماد المذكرات** (`LegalMemo`, `MemoReview`): صفحة `/memos` (قائمة مفلترة + بطاقات ملخص حسب الدور)، `/memos/new` و`/memos/[id]` (كتابة/تعديل للباحث، مراجعة/اعتماد/تقديم للمحامي، سجل مراجعات)، 5 مسارات API (list/create, detail/edit, submit, review, court-submit)، وقسم "المذكرات" في صفحة القضية. مُختبَر عبر HTTP: دورة كاملة (كتابة ← إرسال ← طلب تعديلات ← إعادة إرسال ← اعتماد ← تقديم للمحكمة) + كل حواجز الأدوار.
- **نظام الإشعارات الكامل** (`Notification`, `NotificationPreference`، 38 نوعًا/4 قنوات): بنية قناة-جاهزة (in_app فعّال + email/sms/whatsapp stubs)، دالة موحّدة `sendNotification` + أغلفة آمنة، مُطلقات مدمجة في مسارات الاستلام/المذكرات/القضايا/الجلسات/المهام، مهام مجدولة (`checkTimeSensitiveNotifications` + `/api/cron/notifications` بـ `CRON_SECRET`)، جرس polling (30ث) بـ dropdown+toast، صفحة `/notifications`، صفحة تفضيلات `/settings/notifications`، وأداة `/dev/trigger-reminders`. القرار: Polling لا SSE (انظر القسم 4). مُختبَر عبر HTTP: 8 سيناريوهات (استلام/مذكرة/إغلاق/جرس/قراءة/تفضيلات/كرون).
- **نظام التعريفات الموحّد** (`fieldDefinitions` + `FieldTooltip`/`DefinedField`): قاموس مركزي لكل المصطلحات القانونية الغامضة، tooltips بالتحويم والنقر (بلا مكتبة خارجية) مطبّقة في النماذج، صفحة `/glossary` مرجعية بالبحث والفئات، ورسائل خطأ تعليمية موحّدة (`VALIDATION_MESSAGES`). مُتحقَّق بصريًا (القاموس + tooltip الطرف المقابل).
- `npm run build` نظيف بدون أخطاء TypeScript أو تحذيرات بناء.

## 6. ما تبقى من مهام (خارطة الطريق)

بالترتيب المقترح:

1. **التقويم العدلي**: مزامنة/استيراد مواعيد الجلسات مع التقويم الرسمي (ناجز/معين) وربطها بجلسات النظام تلقائيًا.
2. **الخدمات القانونية**: كيان منفصل للاستشارات/الخدمات غير المتقاضية (عقود، مراجعات، آراء) بمسار وفوترة مستقلين عن القضايا.
3. **Cloudflare R2 / تخزين سحابي**: ترحيل رفع المستندات (`public/uploads` و`public/uploads/intake`) إلى تخزين سحابي قبل أي نشر متعدد الخوادم — **لا يوجد `@aws-sdk/client-s3` ولا `src/lib/r2.ts` حاليًا**، مع تعليق `TODO` في نقاط الكتابة.
4. **النشر (Deployment)**: إعداد بيئة الإنتاج (قاعدة بيانات مُدارة، متغيرات البيئة، `NEXTAUTH_SECRET`، `CRON_SECRET`، جدولة `/api/cron/notifications`، تشغيل Puppeteer على الخادم، وCI). كل التطوير حتى الآن محلي.

> **مُنجز حديثًا** (خرج من خارطة الطريق): **نظام المهام (Tasks)** — كيان مهام كامل قابل للإسناد داخل القضية/الطلب أو مستقل (انظر القسم 4 و5).

### نواقص أصغر معروفة
- تفعيل `twoFaEnabled` (حقل في `User` بلا تنفيذ).
- لا اختبارات آلية (unit/integration) — كل التحقق يدوي عبر `npm run build` وفحص HTTP.
- **سجل التدقيق جزئي**: يسجّل `create`/`update` فقط — لا `view` (فتح المستندات) ولا حذف فعلي (النظام يؤرشف). لا يوجد `DocumentAccessLog`.
- **لا توجد صفحة مستندات مستقلة** (`/documents`) — المستندات ضمن صفحة القضية فقط.

## الملاحظات التقنية المهمة

### بعد أي تعديل على Prisma Schema

⚠️ Next.js dev server لا يُعيد تحميل `@prisma/client` تلقائيًا عند إعادة توليده.

بعد أي واحد من هذه الأوامر:
- `npx prisma migrate dev`
- `npx prisma generate`
- `npx prisma db push`
- `npx prisma migrate reset`

يجب إعادة تشغيل dev server يدويًا:
1. `Ctrl+C` في terminal الخاص بـ `npm run dev`
2. `npm run dev`

الأعراض إذا نسيت:
- Runtime error: `"prisma.X.count is not a function"` (أو `Cannot read properties of undefined`)
- الحقول الجديدة تظهر `undefined`
- API routes ترجع 500 بدون سبب واضح

`npm run build` يستخدم client محدّثًا دائمًا، فالخطأ يظهر فقط في dev.

## 7. لقطة حالة فعلية (فحص 2026-07-13)

فُحص المشروع بالكامل (`prisma migrate status`, `npm run build`, بذر البيانات، وتحقق HTTP للدورات الكاملة). النتيجة:

| البند | الحالة | ملاحظات |
|---|---|---|
| قاعدة البيانات (26 model) | ✅ | لا migrations معلّقة (13 مطبّقة)؛ آخرها `dup_lookup_indexes` |
| فحص التكرار + تحقق صارم | ✅ | جوال 10 خانات، تحذير 409 قابل للتجاوز، بانر استلام حيّ، أداة dev — مُختبَر عبر HTTP |
| الأدوار الفعلية (6 أدوار) | ✅ | استُبدل `partner`/`senior_lawyer` كليًّا؛ 7 مستخدمين مزروعين + تحصين الجلسات |
| نظام المهام (Task/TaskComment) | ✅ | جدول+كانبان، ترقيم `TSK`، صلاحيات إسناد بالفريق، دورة الحالة، مُختبَر عبر HTTP |
| مسار استلام القضية (Intake) | ✅ | دورة كاملة + فحص تعارض آلي + بنك رفضات + **تفويض تقييم + نماذج/مستندات تنتقل للقضية**، مُختبَر عبر HTTP |
| أطراف الدعوى وصفة الموكل | ✅ | `isOurClient`/`clientPartyRole` + بطاقات متقابلة، مُختبَر |
| نظام اعتماد المذكرات | ✅ | دورة كاملة مُختبَرة عبر HTTP + حواجز الأدوار |
| نظام إغلاق القضايا | ✅ | مُختبَر عبر HTTP كاملًا (مسؤول النظام) |
| المسارات القضائية (`CaseFlowStage`) | ✅ | + واجهة إعدادات |
| التسوية الودية (قوى/تراضي) | ✅ | حالة القضية تُزامَن مع النتيجة (إصلاح المنطق المعكوس) |
| الهجري + الأرقام الإنجليزية | ✅ | `moment-hijri` + `toEnglishDigits` |
| النماذج الثمانية + PDF | ✅ | Puppeteer، مُتحقَّق بصريًا (عربي متصل RTL) |
| التقارير (معدل كسب + توزيع نوع/صفة) | ✅ | — |
| صلاحيات العملاء | ✅ | ثغرة الرؤية مُصلَحة ومُختبَرة |
| الجلسات / التدقيق / المستخدمون / الفواتير | ✅ | صفحات مركزية كاملة |
| تغيير كلمة المرور + تحقق الأرقام | ✅ | `/settings/account` + `validators.ts` |
| اكتمال أحداث سجل التدقيق | ⚠️ | create/update فقط، بلا view |
| نظام الإشعارات (Notification/Preference) | ✅ | Polling 30ث، 38 نوعًا، محوّلات قنوات جاهزة للبريد، مُطلقات مدمجة + كرون، جرس+صفحة+تفضيلات، مُختبَر عبر HTTP (8 سيناريوهات) |
| التقويم العدلي / الخدمات القانونية | ❌ | خارطة الطريق (القسم 6) |
| صفحة مستندات مستقلة / `DocumentAccessLog` | ❌ | غير موجودة |
| Cloudflare R2 / النشر / 2FA / اختبارات آلية | ❌ | لاحقًا |
| `npm run build` | ✅ | ينجح بلا أخطاء TypeScript |
